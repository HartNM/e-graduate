# --------------------------------------------------------------------
# General / OS
# --------------------------------------------------------------------
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --------------------------------------------------------------------
# Dependencies
# --------------------------------------------------------------------
# ป้องกัน node_modules ทั้งใน root, backend  และ frontend
node_modules/

# --------------------------------------------------------------------
# Environment Variables
# --------------------------------------------------------------------
.env
.env.development
.env.production
frontend/.env.development
frontend/.env.production

# --------------------------------------------------------------------
# Build Outputs & Distributables
# --------------------------------------------------------------------
# โฟลเดอร์ dist ที่เกิดจากการ build (เห็นใน frontend )
dist/
build/
out/

# --------------------------------------------------------------------
# IDE & Editor Directories
# --------------------------------------------------------------------
# ไฟล์ตั้งค่า IDE (เห็นใน frontend )
.vscode/
.idea/
*.swp
*.swo
.e-graduate.code-workspace

# --------------------------------------------------------------------
# Frontend Specific (Vite / React)
# --------------------------------------------------------------------
# Cache ของ Vite [cite: 304, 305]
.vite/
.vite-temp/

# Testing coverage
coverage/

# --------------------------------------------------------------------
# Backend Specific
# --------------------------------------------------------------------
# หากมีการ build backend เป็น production
backend/dist/
backend/build/

# ไฟล์ชั่วคราวอื่นๆ ที่อาจเกิดขึ้นจากการรัน backend
*.pid
*.seed
*.pid.lock